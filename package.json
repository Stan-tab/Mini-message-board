{
	"name": "mini_message_app",
	"version": "0.0.1",
	"main": "app.js",
	"type": "module",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"install": "npm inst express && npm inst ejs && npm inst dotenv",
		"dev": "node --watch $( node -p \"try { require('./package.json').main } catch(e) {}\")"
	},
	"author": "Stan",
	"license": "ISC",
	"description": "Mini message app for The Odin Project",
	"dependencies": {
		"dotenv": "^17.2.1",
		"ejs": "^3.1.10",
		"express": "^5.1.0"
	}
}
